export const environment = {
    nodeMailerAuth: {
        host: 'HOST',
        port: 0,
        secure: true, // use SSL
        auth: {
            user: 'USER@EMAIL',
            pass: 'PASSWORD'
        }
    }
}